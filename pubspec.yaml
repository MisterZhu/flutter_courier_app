name: courier_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 4.0.0+1

environment:
  sdk: '>=3.2.1 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # 网络
  dio: ^5.4.0
  connectivity_plus: ^2.3.6
  # WebView
  flutter_inappwebview: ^6.0.0
  # 数据存储
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.0.0
  sqflite: ^2.3.2
  # 状态管理
  riverpod: ^2.4.9
  flutter_riverpod: ^2.4.9
  # 路由以及Controller
  get: ^4.6.6
  # UI组件
  flutter_easyloading: ^3.0.5
  easy_refresh: ^3.3.4
  cached_network_image: ^3.3.1
  photo_view: ^0.14.0
  # 环境变量
  flutter_dotenv: ^5.1.0
  # 工具库
  uuid: ^4.3.2
  intl: ^0.19.0
#  image: ^4.1.7
  decimal: ^2.3.3
  encrypt: ^5.0.3
  android_id: ^0.3.6
  #  event_bus: ^2.0.0
  collection: ^1.18.0
  url_launcher: ^6.2.5
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.6
  flutter_scankit: ^2.0.3
  ua_client_hints: ^1.2.2
  device_info_plus: ^10.0.1
  package_info_plus: ^6.0.0
  permission_handler: ^11.2.0
  flutter_image_compress: ^2.2.0
  #  image_picker: ^1.0.7
  wechat_camera_picker: ^4.2.1
  wechat_assets_picker: ^9.0.0-dev.2
  # pda scan
  pda_barcode_scan: ^0.0.1
  # 调试库
  #  talker: ^4.0.3
  #  talker_flutter: ^4.0.3
  #  talker_dio_logger: ^4.0.3
  #  pretty_dio_logger: ^1.1.1
  #  awesome_dio_interceptor: ^1.1.0
  flutter_pretty_dio_logger: ^3.0.0
  #  dokit: ^0.8.2-nullsafety.10
  # google maps
  google_maps_flutter: ^2.5.3
  maps_toolkit: ^3.0.0
  dotted_border: ^2.1.0
  geolocator: ^11.0.0
  signature: ^5.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
  assets:
    - assets/images/add_pictures_circle.png
    - assets/images/app_error_nowifi.png
    - assets/images/arrow_right.png
    - assets/images/base_bg.png
    - assets/images/copy.png
    - assets/images/delivery_bg.png
    - assets/images/dialog_close.png
    - assets/images/dispatch_pick.png
    - assets/images/down_arrow.png
    - assets/images/drag_handle.png
    - assets/images/file_delete.png
    - assets/images/file_upload_add.png
    - assets/images/fold.png
    - assets/images/hide_password.png
    - assets/images/home_bg.png
    - assets/images/list_arrow.png
    - assets/images/list_arrow_black.png
    - assets/images/local.png
    - assets/images/login_logo.png
    - assets/images/map_cancel.png
    - assets/images/map_check.png
    - assets/images/map_check_disable.png
    - assets/images/map_dingwei.png
    - assets/images/map_dingwei_off.png
    - assets/images/map_draw.png
    - assets/images/map_draw_min.png
    - assets/images/map_position.png
    - assets/images/map_position_on.png
    - assets/images/map_current_position.png
    - assets/images/map_redraw.png
    - assets/images/map_redraw_disable.png
    - assets/images/map_return.png
    - assets/images/map_scan.png
    - assets/images/map_search.png
    - assets/images/map_dispatch_icon.png
    - assets/images/map_delivery_icon.png
    - assets/images/mission_claim.png
    - assets/images/nav_back.png
    - assets/images/off_duty.png
    - assets/images/on_duty.png
    - assets/images/order_checked.png
    - assets/images/order_management_bg.png
    - assets/images/order_unchecked.png
    - assets/images/phone_call.png
    - assets/images/primary_check.png
    - assets/images/primary_close.png
    - assets/images/returning_list.png
    - assets/images/route_guide_fail.png
    - assets/images/route_guide_ok.png
    - assets/images/Scan.png
    - assets/images/scan_box.png
    - assets/images/scan_code.png
    - assets/images/scan_code_ps.png
    - assets/images/seach_icon.png
    - assets/images/search_icon_main.png
    - assets/images/selected_left.png
    - assets/images/selected_right.png
    - assets/images/show_password.png
    - assets/images/tab_account.png
    - assets/images/tab_account_selected.png
    - assets/images/tab_home.png
    - assets/images/tab_home_selected.png
    - assets/images/tab_task.png
    - assets/images/tab_task_selected.png
    - assets/images/unfold.png
    - assets/images/up_arrow.png
    - assets/images/warning.png
    - assets/images/whats_app.png
    # environments
    - .env
